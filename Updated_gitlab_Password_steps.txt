ssh username@IP address
Username="integra"
Example IP="111.11.111.111"
username@IP address's password:********* // Can't be view 
then it will connect 

cd/ect/gitLab/ ---> move to the specific directory
need to change the password of the gitLab.rb file 
for that need to give the command like 
nano gitLab.rb
ctrl+w --> need to find the ldab
then change the password
then ctrl+o -->write & ctrl+x

need to reconfigure 
sudo gitlab-ctl reconfigure
then need to enter password for that
